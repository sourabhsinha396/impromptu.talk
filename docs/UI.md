# UI

The rebuild is the chance to improve every screen, and this is the record of it. Behaviour, rules and copy come from v0 and `SPEC.md`; the look does not. Before a card builds a surface, a standalone mock of it lands under `docs/mocks/` as one HTML file that opens on its own (real tokens, real type, the light and dark themes, a phone width), the owner approves it, and the row below is filled in. A surface without an approved mock is not built.

What every mock has to keep: the ten-second rule (home is the tool, near-zero words, one primary button, everything else behind the account menu), the tokens in `frontend/app/globals.css` (the accent colours what you read, the button is ink), lucide glyphs, and type sized for a phone filming a laptop.

| Surface | Cards | Mock | What improves over v0 | Status |
|---|---|---|---|---|
| Home and the round (idle, topic, prep, speak, done) | 08, 11, 12, 13 | `mocks/home.html` | Idle keeps v0's shape (chip and gear above the question, the question in the accent stating the speaking length, one button that says Spin). Inside the round: Reset as an underlined link under the buttons instead of a back arrow; Spin as a button carrying the die instead of a rotate icon; the primary button names the duration; one ring clock in the same place for thinking and speaking, warm in the last ten seconds; notes become chips; done is three tiles with Spin again leading; wider spacing throughout. Built on cards 08, 11 and 12 with two copy calls logged in DECISIONS: the done headline follows v0 ("Day N." or "Nice."), and the speak screen's Reset says it keeps nothing. | approved 2026-09-06, built; the sheets (the last three mock tabs) built on card 13 and added to the mock after. On 2026-09-08 the settings sheet became two pages on the owner's call, the shape `/account` and `/account/additional-settings` already have: the front page is what gets changed before a round (the two lengths, the style, feedback), and a row at its foot opens Additional settings, which holds what is set once and forgotten. Mute sound effects moved there, and an influencer's page settings land there next. Approved from the mock the same day |
| Genres hub and genre page | 09 | `mocks/genres.html` | Hub as ten cards with the topic count on each and one button into the tool; the genre page groups topics by style under real headings, two columns on a laptop, one button that opens the tool with the genre picked, More genres at the foot. | approved 2026-09-06 |
| Chrome: header, account menu, footer | 05 | in both mocks | As built on card 05: the die and wordmark, the streak pill, the account button; footer with the owner and three columns. | approved 2026-09-06 (as part of both mocks) |
| Streak page | 17 | `mocks/streak.html` | The three tiles are the done screen's tiles, so the page the flame opens looks like the screen that sent you. Free gets a five-day strip of named days (the rule is five days) rather than a five-square heatmap; the year heatmap is Pro's, with a dashed square for a day the freeze held and the month names over the columns. Recent shows the genre glyph beside the time. The pitch is one sentence, only when true. Share sits after the primary button. One column at 960px, the genre page's frame. List lengths in the mock are shortened; the real caps are 25 runs free and 1000 Pro. | approved 2026-09-07 with two changes by the owner: the tiles are a three-column grid that never wraps two and one, with no glyph on any tile; every line of copy is one short sentence in plain words. Built on card 17. |
| Share page | 18 | `mocks/share.html` | The streak page's shape seen by a stranger: the same label, headline, three tiles and heatmap, with no pitch, no times and no footer line. Three tiles rather than v0's five. Eight weeks for everybody, its own constant. Only bank topics named, each a link into a round. The name if given, never the email. One button, "Try one yourself", then the tagline. | approved 2026-09-07; then, on the owner's UX note the same day, all three tiles carry a glyph on a laptop (flame, topics, minutes; none on a phone), the tiles span the column, the calendar's cells size themselves to the room, and the calendar and the list sit side by side on a laptop when the calendar is eight weeks or shorter. Applied to the streak page and the done screen too. Built on card 18. |
| Sign up, sign in, forgot, reset | 19, 20, 21, 22 | `mocks/signin.html` | One narrow column (440px), the headline in the display face, one sentence under it, the form, one line out. The fields are the settings sheet's, so a page and a sheet share one kind of input. The way out sits on the label row ("Forgotten?", "8 or more characters"). A refusal is one sentence above the form with an ink edge, the typing stays; a notice has an accent edge. Name is optional. One full-width ink button. Google and the captcha are drawn where they land (cards 21, 22) and are absent until their keys are set. Forgot and reset share the frame. | awaiting the owner's word; sign up and sign in built from it on card 19 and forgot and reset on card 20, ahead of that, changes to follow |
| Account pages | 23 | `mocks/account.html` | Every section is a card with a heading, one sentence, its control and its own Save, where v0 stacked a grey label, a form and a hint with nothing around them. The page says who you are: the email and a Pro or Free tag under the headline, which v0's `/account` never showed at all. Two pages under one tab bar, replacing v0's section at the foot of `/account` whose only content was a button to a page of the same name; the second page is `/account/additional-settings`, since "additional" alone reads as a fragment. Feedback lands in the card that did the work rather than in one banner at the top after a reload, and Save stays off until something changes. The six accents are swatches that preview on the page at once, not v0's dropdown of colour names, and only Save is Pro's. Purchases show the amount and a refunded tag. The affiliate and share links are fields with a Copy button. "Sign out everywhere" asks first, because it ends this session too. Sharing is a switch with the live link beside it. The order on `/account` stays v0's: subscription, purchases, affiliate link, colour. | approved 2026-09-07 |
| Receipt page | 26 | `mocks/done.html` | One column, and the first line is what somebody came for: "You have Pro." before any paperwork. The plan, the money and the reference as rows, the account page's shape. The primary button is a round, not the account page: somebody who has just bought practice should be practising ten seconds later. Pending is a real state and says so plainly, because with no webhooks a buyer who closes the tab lands there and the link in their receipt settles it later; v0 left that looking like a failure. Failed says nothing was charged. The reference is monospaced, since the only reason it is on the page is that somebody may have to paste it into an email. | approved 2026-09-07 |
| Pricing page and checkout | 25 | `mocks/pro.html` | Cards first and the list of what Pro holds under them, belonging to whichever plan is picked, where v0 printed the same seven lines inside both cards. Only two of its six lines actually differ between plans (the streak and the history the plan tracks), so the rest are said once. The pill is a segment rather than a select, since both options are worth seeing. Free gets a line said out loud, which v0 had written and then commented out. Currency is a dropdown of codes with flags on the Plans row, because it changes every number below it. The questions are shadcn's accordion. Much less prose: the headline, one line, then the numbers. And the typos are gone: v0 shipped "Steak survives 2 missed days" and "Tacks 365 days of streak" on the live page. | approved 2026-09-07 with three changes by the owner: cards above and benefits below, "Plans" rather than "Pick a shape", a flag dropdown of currency codes, and the one-time pill reads "30 day pass" rather than v0's second "Monthly". Built on card 25 |
| Owned genres: list, editor, shared page | 29, 30 | `mocks/yours.html` | They are genres, not packs: one table holds the bank and the ones people write, so the picker has one row shape and the editor one vocabulary, and the routes sit under the hub that already exists (`/genres/yours`, `/genres/yours/[slug]`, and `/g/[token]` for a shared one). The list is the genres hub's own cards with a pencil and a dashed "Make one" in the same grid, so the thing you make appears where you were already looking, and the header says "3 of 10", which is the cap said once instead of a refusal later. Adding is one card with a segment, Paste a list and Describe them (card 30), where v0 had "build, import, CSV" as three names for typing lines into a field: paste is the default because it always works, and the generation count sits on the card rather than arriving as a refusal after the click. A topic is edited in place, the row becoming a field and a style box that takes one of ours or words of your own, where v0 opened a modal with a select and a second hidden field. Sharing is the streak page's switch with the live link beside it and a line saying that turning it off kills the link people already have. Without Pro nothing is hidden: the genres stay listed, practisable and shared, and only making, pasting and editing say what they need. | approved 2026-09-07 with one change by the owner: the generate button reads "Generate 20 topics". Built on cards 29 and 30 |
| Genre picker: making and managing your own | 13, 29 | `mocks/genre-picker.html` | Five drawings of the same moment, of which D is built. What was wrong: a genre saved under the pencil icon made the identity glyph read as an edit button, so one column meant two things; "Make your own genre" was drawn exactly like a row that picks, though it leaves the round; and once you had a genre there was no door to edit it. D answers the three questions where each is asked - the heading edits the group and carries the cap as a fact ("Yours &middot; 3 of 10"), a pencil at the right edge edits one genre and is always drawn because touch has no hover, and the row at the foot means only "a new one" and wears a chevron because it leaves. An action never sits in the identity column. With nothing of your own, all of it collapses to one row and one line that explains the feature once. | approved 2026-09-07 (owner's mock), built |
| Affiliates | 31 | `mocks/affiliate.html` | Every number on the page is read from the backend - the rate, the sixty-day window and the minimum payout come from the same constants settlement pays under, where v0 printed the rate in three places and the FAQ once disagreed with the code. The pitch is three steps rather than three paragraphs, so the whole product fits above the questions, and the link is a field with a Copy button, the control the account page and the share switch already use. The referrals page is three tiles that never wrap two and one (people, bought, balance), with earned and paid under them as one sentence, because they are how the balance was reached rather than separate facts. Nobody on the activity list is named, a refund is its own line rather than a sale quietly vanishing, and the sample faces say they are samples under each face instead of in a footnote at the bottom. | approved 2026-09-07, built on card 31 |
| Administration | 32 | `mocks/administration.html` | The hub is the genres hub's cards, so a tool is a place you go rather than a link in a menu. Staged topic picks the genre first, because the topic list is a thousand otherwise, and it says out loud that it wrote to this browser only. Giving Pro is one form with two lengths labelled by how long rather than by the plan's name, and the live gifts sit under it with Take it back on each; the page says why a paid purchase is not in that list. Payouts lead with the arithmetic the admin cannot show - who is owed what, most owed first, with a missing PayPal address called out as the support mail it is - and the amount is typed rather than filled in, since the balance is a suggestion and not an instruction. Outreach shows the addressed message ready to copy with the rate read off the same constant settlement pays, and the table under it is the memory of who has already had it. | approved 2026-09-07, built on card 32 |
| About, contact, legal | 33 | `mocks/pages.html` | One document frame for all five, 720px wide with prose at 66 characters a line, where v0 gave each page its own width and heading scale. Every number in the copy is v1's: a thousand topics across ten genres, four styles, a five day streak, where v0's about page said 800 across 20 and was wrong the day it shipped. The affiliate rate in the terms is read from the backend, the one number in this prose that money depends on. Contact is a mailto and the chat bubble with no form, and the page says why. Refunds names which shapes renew before it says what cancelling does, and never carries v0's "nothing renews", which was false for exactly the people it mattered to. Privacy says the new things out loud: the five cookies, that a shared streak or genre shows a name and never an address, that a prompt typed into the generator reaches a model, and that analytics run on the live site only. | approved 2026-09-07, built on card 33; revised the same day on the owner's note that the headlines were cheesy - each page is now titled as the document it is, and the refund page no longer explains the product |
| Done screen: the report | (new) | none, built against the live pipeline on the owner's call | The minute you just spoke, drawn as one bar above the three tiles. Talking is the accent, a gap past 1.5 seconds is the warm colour the clock already uses for its last ten seconds, an ordinary breath is a line, and silence after the last word keeps the track's own background, because answering in twenty seconds is a short answer and not a forty-second hole. The bar comes before the tiles: the streak is already in the header pill on every page, so the done screen's own job is to say something about the sixty seconds that just happened. Under it one sentence, chosen from whichever number is furthest from where it should be, then the plain numbers, then the words you leaned on. Never a paragraph and never a rewrite of anybody's sentence: prose from a model cannot be plotted, so a report made of it would mean the progress view could never exist. A round with no microphone draws nothing at all rather than apologising for a feature nobody asked for. | built 2026-09-07 without a mock, on the owner's call ("no need to draw mock ui, keep it real"), and verified against the live pipeline in the browser rather than on a static page. On 2026-09-08 a ping was added after "See the full report" (owner's call): the done screen has several pressable things and that link is the one nobody finds, so a dot marks it. `components/site/ping.tsx`, the first shared component made for one call site, with the colour taken by name from the token set and never as a hex, and the ring moving only under `motion-safe` |
| Your round, read back | (new) | `mocks/round.html` | The record of one round at `/streak/[id]`, where the done screen stays a moment and this page is the reading. Everything on it is arithmetic over what is already stored for a Pro round, the word timings above all: each um drawn under the minute at the second it was said; pace as a line through the round in ten-second steps against the shaded 130 to 170 band, because one number hid a sprint at 0:20 and a fade from 0:40 in the same round; the shape of the round as a radar (owner's screenshot) with a dashed comfortable pentagon, today filled and your usual as a thin outline, beside the same five measures as bars in their real units with a hollow ring for your usual; the words you leaned on as a donut with the total in the centre, fillers warm and leaned-on words in the accent, with every count listed, replacing the ums-a-minute band (owner's call: a cluster you can see needs no rate beside it); phrases said more than once and restarts, a two-word run begun twice within four seconds; one thin column per sentence with the run-on in warm; three tiles for the facts that are not a scale; and the read-back open, with restarts dotted underneath. Taken from ReUI's chart set: the donut with a centre total and its hover variant, the filled radar, the heading-with-one-number card pattern. Left there: glow, entrance motion, stripe fills, the concentric radial, forecast zones. Every chart is Recharts, which `/streak` already loads. Two data findings came out of drawing it from run 30 and are written in the mock's notes: the browser clock ran four times fast on that round because every respin started another level ticker, and pace is measured over speaking time but judged against a wall-clock band, so nearly every round reads as rushed. Rejected from the transcriber's menu: sentiment, key phrases, entities, categories, summaries, and per-word confidence, which would grade an accent. The free tab is drawn with Groq asked for word timings, which it gives at the same price. | approved 2026-09-07 and built the same day on card 35. During the build the owner sent a sketch and the minute became a wave on both screens: strokes for a voice, a flat line for silence, the holes in warm; the mock was redrawn to match |
| Progress on the streak page | 36 | `mocks/progress.html` | The foot of `/streak`, drawn for the person practising after the owner asked who would pay to know their ums a minute. Then and now: the first rounds against the last, one row per skill (silence, time to start, restarts, filling the time, a clean ending, the word you lean on most, different words), a hollow dot for then and a filled one for now on the comfortable stretch, one plain sentence each. Work on next: the skill furthest from comfortable over the last five rounds, with the round that shows it worst and one thing to do. Your floor: the quietest of the last seven rounds against the first seven's average. Three lines over the window (silence, restarts, time to start) with the band shaded, a dot per round up to twelve, and "was 27s · better" rather than up or down. By genre with the hardest named and one line on prep. Firsts, dated and linked. The first and latest minute as waves with their dates. Free keeps the lines and the waves and sees one sentence for the rest, as the calendar does. The first draft kept the three old cards and only added the band; the owner asked what a learner would actually want, and the page was redrawn around skills. | approved 2026-09-07 and built the same day on card 36 |
| The case you made, and the row under progress | 36 | `mocks/argument.html` | Everything on the round page said how somebody spoke and nothing could say whether they did what the topic asked; run 30 got a pen fight story for "Argue for owning one good pen" and the page reported 143 wpm. A new first section, above pace: the verdict (a word from code in the accent, ink or warm, then one model sentence), every sentence as a block on the minute coloured by its role (the point, a reason, an example, an aside, a wind-up, the close), the point flagged at its second or "no point made" at the right, a key of only the roles used, and one next-time sentence that is not behind a disclosure. On the streak page, one row of two cards under the three trend cards: answered the topic as ten squares for the last ten rounds, and time to your point as the same trend card as time to start, under 12 seconds shaded. The model is asked for structure (a role per sentence, yes, half or no, two capped sentences) and never a score, a rewrite or a paragraph; all the drawing is arithmetic over the roles and the word timings already stored. Pro only, once per round when the report attaches, stored and never recomputed. Free sees the same section and row drawn from one fixed sample round, blurred, with the streak page's pitch line: "Pro reads what you said." Not on the done screen, which stays a moment. | approved 2026-09-07 ("lets implement it") and built the same day on card 36. Two changes during the build, both in DECISIONS: the round page's section header carries no count, since that number lives on the progress card where the ten rounds are; and the sample's blurred half is `aria-hidden`, so a screen reader is never read somebody else's minute as though it were their own |
| Proof the round is being heard | (new) | `mocks/recording.html` | The microphone's health is said during the round instead of after it. Today a microphone that opened and heard nothing is only reported on the done screen, sixty seconds too late, and one that never opened at all draws nothing at all, on that round and on every round after it: for the whole minute a working round and a broken one look identical, so there is no moment at which anybody could have fixed it. Three states, three places. **Heard** is a level meter under the clock, five bars on the centre line the report's wave is already drawn on, moving with the voice, with no label and no icon, because a meter that is moving is already the sentence. **Open but silent** (muted hardware, the wrong input, a headset that never connected) is caught on the topic screen before the clock, since the level ticker is already running there and a live microphone in a silent room is never digital silence: exact zero for two seconds is a dead input and not a quiet room. **Heard nothing once you began** is caught four seconds into the speaking, when the bars have not moved: they go warm and flat and one sentence says so, and the clock never stops, because a broken microphone is not a broken round. Nothing new is measured; the level is already sampled fifty times a second for the timeline and this only draws it. The two indicators that were drawn and not taken are kept in the mock's fourth tab: the report's own wave drawn live, which would show somebody their pauses while they are still in them, and a breathing pulse, which says something is on but not that it is hearing you. | approved 2026-09-08 and built the same day on card 37; the meter was the owner's call over the wave and the pulse, and its scale changed during the build (see DECISIONS): full height is a shout and the climb to it is a square root, because a linear meter read dead for exactly the person talking normally |
| The report, sized for filming | (new) | `mocks/report-filming.html` | People film this: a laptop held up to a phone, or a phone on a stand pointed at the screen, and the reel is a vertical crop of the middle of it. Today's report is drawn for somebody reading a laptop at arm's length - a 40 pixel wave, 12.5 pixel numbers, four bands in two columns - and inside that crop, at the scale a camera and a compressor leave, none of it is legible. So every measure becomes a card, nothing is thinner than four pixels, colour carries the verdict rather than grey text, and the whole reading is shown rather than the summary: the minute, whether the round answered the topic, the shape against your usual, the four measures, the words you lean on, restarts and sentences, all of it arithmetic over what the round already stores. The board is ten cards on one laptop screen with no scrolling; the column is the same cards at the width the 9:16 crop leaves, which is what a phone gets anyway, so it is one design and not two. The read-back stays a link, because a transcript cannot be read on camera at any size. Turned on by `Report sized for filming` on the sheet's Additional settings page - not a zoom, so not "big fonts". The mock carries two tools for judging it, a 9:16 crop guide and a 40 percent "as filmed" view. | approved 2026-09-08 and built the same day. During the build the wave's pitch doubled with its stroke weight, because strokes three times as thick at the old spacing merge into a block on a phone, and the minute card's line became the minute's own facts rather than the report's headline, which the gap card was already carrying |
| Waiting for the report | (new) | `mocks/waiting.html` | The done screen sits still for two to fifteen seconds while the audio uploads and comes back read, and today that wait is a hairline in `--line` pulsing between full and half opacity: about one percent of contrast on white, so nothing appears to move and the screen reads as finished and broken rather than as busy. Three ways out, drawn on both report drawings and animating for real, with a toggle for what lands so the jump can be judged. **1, the minute already drawn:** the browser measured the speech and the silence itself and is uploading that timeline, so it can draw the wave the instant the round ends, dimmed, with a read head crossing it left to right and the strokes brightening behind - the wave is not a placeholder, it is the half of the report that needs no network, already at its final size, so nothing moves when the rest arrives. **2, the report's own frame:** the finished layout as ghosts with the card titles in real ink, since the titles are known before any request is made. **3, one line that moves:** today's shape with the bar travelling in the accent instead of pulsing in the hairline, the upload's true percentage while it is the upload, indeterminate after that because there is one request in flight and no honest way to know how far through it is, and after twelve seconds the sentence that answers the only real question: your streak is already saved. | approved 2026-09-08 and built the same day: the owner chose the frame. Two things changed in the build. The frame names nothing, though the mock named every card: half the measures need a transcript and the verdict needs Pro, so a placeholder with "Pace" or "Answered the topic" on it is a promise broken on every free round, while an unnamed shape claims only that a report of about this size is coming. And what the browser already holds is drawn for real rather than as a placeholder - the clock on both drawings, and on the filming board the topic and the day, which is what stops the board jumping down a line when the reading lands |
| The shape card, made readable | (new) | `mocks/shape.html` | The radar was the one drawing on the site nobody could read. On the filming board it had no axis labels and no key, so five measures read as one olive blob, and the caption under it already said the whole thing in six words; it also summarised cards sitting two inches from it, since all five measures have their own card on that board. The mock is a gallery: ten drawings of that same number, each at the size it takes on the board, click one to see it in place. **The bar family** - columns (a radar is a bar chart bent into a circle, so this is that circle cut open), the same chart on its side, and a segmented tally; they share the cost that bar height is a score and not a number anybody can quote. **The pie family** - a broken pie of five equal slices, the same pie folded to a dial, and a plain ring four fifths full; they share the cost that slice position means nothing and none of them can name what went wrong. **Not charts** - named marks, two numbers side by side, or no card at all. The round page's own copy of the radar is drawn beside the very bars it is a picture of, and the mock offers three replacements there: named zones (the three stretches drawn along each track with the one you landed in named under it), the bar chart at page size, or a measure a row with a line of your last ten rounds. | the board card approved 2026-09-08 and built the same day: the owner took the bars on their side. Two changes in the build - the rows are named by `short` and not `label`, because the board already titles four cards with those names and saying each twice costs the bars their width; and `short` stopped being a goal ("Good pace") and became the plain name ("Pace"), because a full bar beside "Good pace" says the verdict twice. The round page is untouched and its radar still stands, awaiting the owner's word |
| What replaces the radar | `mocks/shape.html` | `mocks/shape-card.html` | The shape card was drawn again for the round page alone, with the round's own numbers in it, after the owner read the radar's Fillers axis as "a lot of fillers" when the point at the outer edge meant hardly any. Four drawings, all five measures, all real arithmetic: **A zoned bars**, the radar gone and each track cut into its named zones with the one you landed in bold; **B five dials**, a half arc each with the good stretch lit, the number inside and one word under it; **C the count and five pills**, not a chart at all; **D today against your usual**, two dots joined on one track a measure. | approved 2026-09-08 and built the same day: the owner chose the dials. The bars stay underneath them, so the round page keeps where inside the range it landed and your usual, which is what a dial cannot show |
| The empty half of the streak page | (new) | `mocks/streak-layout.html` | The calendar and the Recent list share a row on a laptop, on the premise written above `TwoColumns`: that the calendar is the wide thing and the list is the tall one, so the two finish together. They never do. A thirty-day calendar is five columns of seven squares, about 180 pixels tall, and it does not grow; the list grows by a row a round, and free lists the last 25 while Pro lists up to a thousand. So the gap between them is the streak - the better somebody practises, the emptier their page looks - and it is not bounded. The same pairing is wrong at every window: five days is a strip of named cards beside a 25-row list, thirty days is the block in the screenshot, and a year is the only window that escapes, because `isWide` gives it the whole width and puts the list underneath. Three ways out: **1** one column, the calendar always full width with a month drawn as one row of thirty days rather than a five by seven block, and the list under it in two columns, which takes out `TwoColumns` and `isWide` both; **2** the calendar rides up beside the three tiles, since it is a small object among small objects, and the list has the page to itself; **3** keep the two columns and cut the list to the last eight with a way to the rest, which the thousand-row cap needs whatever else happens. The mock draws the page at 19 rounds and at 60, and marks the void. | drawn 2026-09-08, awaiting the owner's word; nothing built yet |
| The words you used, by size | (new) | `mocks/words.html` | The whole minute as a word cloud, counted from a real round ("Why cats are plotting something" at `/streak/124`, 111 words), so every size and shade is a count. **Every word is kept** - pronouns, conjunctions, articles, all of it (owner's call): a cloud that hides "the" and "and" is a cloud of a minute nobody spoke. Size is the count on a square-root scale, because linear sizing gives the top word the whole box and the rest a hedge. Shade is the count as well, five steps down one accent ramp from `--accent-strong` to a mix toward `--muted`, so the picture still belongs to whoever picked their accent and never needs a second hue; saying it twice, in size and in shade, is what gives the cloud a middle and edges instead of a uniform field. Words sit across or turned a quarter and never in between, which is the only rotation somebody can read without tilting their head. Packed on a spiral from the middle out, biggest first, off a fixed seed, so the same round draws the same cloud every visit. Nothing needs the backend: the transcript is already on the round, so the browser counts its own words the way it already draws its own wave. Two things the mock is careful to say - where a word sits carries nothing, and a minute is a small sample, so most words are said once and the bottom of the ramp is all ties. The sized line stays in the mockbar as what a phone and a screen reader get: the same counts, biggest first, with nothing packed. | approved 2026-09-08 and built the same day: the cloud, with fillers warm. The sized line was not built - it is kept in the mock as what a narrow screen would want if the cloud turns out not to survive one. The cloud is its own section, "Words you used", under the one that counts what somebody leans on, because the two answer different questions; folding them together is the obvious next move |
| Charts on the shared page | (new) | none; the two drawings are already approved on `/streak`, so it was built live and shown to the owner in the real page | `/s/<token>` had the tiles, the calendar and the topic list, all of which say somebody turned up and none of which say they got better. It now carries two things from the streak page's progress section and nothing else from it: the first minute above the latest as waves, and the three lines (silence, restarts, time to start) as cards. The waves come first here and last on `/streak` - on that page somebody is reading about themselves and has already read four things, while here a stranger is a second in and the waves are the one drawing on the site that needs no label read, which is the same reason the done screen puts the bar above the tiles. The section is called "Getting better" rather than "Progress", because a stranger reads the plainer name. The window is the page's own eight weeks, said under the heading as "over the same eight weeks", so the lines and the calendar above them cannot describe different stretches of time. Nothing that is read by name or by date came with them: no rounds, no genres, no habit words, no milestones, no run ids. | built 2026-09-08 on the owner's ask ("bring in some graphs, it will look better and cool") |
| The challenge link | (new) | `mocks/challenge.html` | A dare sent to one person, and the cheapest of the growth ideas because `/?topic=<slug>` already hands somebody one exact prompt and skips the spinner (§3). Five drawings: the composer, the receiver's screen with a sender's name and without, the minute with the word still on screen, and the done screen that checks it. Three things the mock is arguing. **The receiver's screen is not a new page**: it is the topic phase of home with one line above it, because a stranger landing on it has never heard of the site and the ten-second rule applies at full force; and it carries no Spin button, since spinning is what the dare exists to prevent, so the way out is one quiet link that doubles as the conversion path. **The word they have to say** is what makes it a dare rather than a link, and it is checked rather than announced: the transcript and word timings are already stored, so "you said it at 0:34" is arithmetic, not a model call. **The link is stateless** - topic, word, length and name as query parameters, no token, no row, no table, no expiry - which is the version that adds nothing to the backend. Nothing comes back to the sender and nothing is published; the receiver's done screen offers "Dare them back" and the chat thread does the social half, so the never list stays intact (§14: no leaderboards, no feed). Free at both ends, since a growth surface with a price on it is not one. | drawn 2026-09-09, awaiting the owner's word; nothing built yet. Three calls left open in the mock: whether the sender's name rides in the URL at all (recommended: taken from the account when signed in, so the nicer link is the one an account gets), which button leads on the receiver's done screen, and whether an owned genre's topic can be dared |
| Exam and profession genres | (new) | `mocks/occasions.html` | IELTS, PTE, job interviews and table topics as genre rows, which is the shape `SPEC.md` §3 already settled ("an occasion is a context, and a context is a genre") at the price `PRICING.md` §2 already settled (every built-in genre free forever, "including any exam or profession genre that ever exists, because those pages are the growth engine"). So the mock decides nothing about tiers and only one thing about layout, which it draws both ways. **Grouped** (tabs 1 and 3): the ten stay a flat unheaded list exactly as decided, and the four sit under a "Practise for" heading - not a new pattern, since the picker already carries a labelled group for Yours (§8), so the shelf exists and this is its second tenant. **Flat fourteen** (tab 4) is drawn so the cost is visible rather than argued: fourteen is inside the "somewhere around fifteen" limit but spends all of it, and the real damage is not length but that "IELTS" and "Culture" are not peers, so a flat list asks somebody to choose between a subject and a situation. The exam page adds exactly one thing a genre page does not have: a line saying the tool's own defaults already are the test's format, a minute to prepare and up to two to speak, which is not marketing but the settings in §4 and the strongest sentence available to somebody arriving from a search engine. Topics still group by the four styles rather than by the exam's own parts, because grouping by part would need a second axis on `Topic`; deliberately not proposed. | drawn 2026-09-09, awaiting the owner's word; nothing built yet. Recommended: grouped. Three things the owner has to call: the names, since IELTS, PTE and Toastmasters are other organisations' trademarks and the drawn page carries a plain line at the foot disclaiming affiliation (and the fourth genre is drawn as "Table topics" rather than the club's name for the same reason); how many, since the floor is 40 topics a genre (§3) and four genres is 160 topics to write before any of it ships; and the icons, which must come from the fixed set of 26 |
| A beam round the report button | (new) | none; one component on a control already approved, verified against the real stylesheet in the browser | "See the full report" is the one thing on the done screen nobody was finding. It already had a ping, and the beam says the same thing at the size of the whole control rather than of a dot. Written rather than installed, on the owner's ask for minimal dependency: `offset-distance` is a length, so CSS interpolates it on its own, where the library version needs a JavaScript animation runtime on the screen every round ends on. The light travels the control's own perimeter, not a conic gradient turning behind it - drawn that way first, and on a wide pill it crawled round the two round ends and raced along the edges, reading as two glowing caps rather than as a beam. The colour is the accent by name, so it follows whichever of the six somebody picked, and it runs only under `motion-safe`. Slower and thicker on the filming board than on the done screen, because that one is filmed and a fast sweep is what a compressor smears. | built 2026-09-08 on the owner's ask; the ping is still there beside it, and whether the button wants both is open |
| Image impromptu (the picture round) | (new) | `mocks/image-topic.html`, partly | A photograph in place of the sentence, in every genre, and otherwise the same round. Three things changed between the mock and the build. **The Words/Pictures toggle on the home stage is dropped** (owner's call: the landing page must not get heavier): the mode is a checkbox in the settings sheet beside the style, because it answers the same question - what you are handed when you spin - and home stays the chip, the question and one button. `/features` is the way in, linking to `/?pictures=1`. **The caption is dropped**: the mock drew a line over the picture, but the topic's own `text` already is the prompt, and one string shown, recorded and read back beats two. So a picture round is literally the text round with a picture above the same sentence, which is what the owner asked for. **On the timed screens the picture sits beside the prompt, not above it** - the mock never drew those, and stacked it cost 132px and pushed the notes and buttons off a 1280x800 laptop, on a screen already 34px tight before any picture. Beside, prep measures 810px against 835px for a text round, so a picture round now fits better than a sentence one, and the ring never moves. The picture gets no reel: a strip of eleven sentences rolling to a stop on a photograph promises the wrong kind of answer. | built 2026-09-09. Twenty pictures, two per genre, WebP in `frontend/public/topics/` (2.3MB, 118KB average), sourced from Unsplash. Prompts were written after looking at each picture, and none speculates about an identifiable person in the frame. Custom picture genres (upload your own) are the next card and are not built |
| The picture reel | (new) | none; the sentence reel it copies is already approved in `mocks/home.html`, so it was built against the real component | A picture round spins like any other. The strip, the `SPIN_MS` timing and the settle contract are the sentence reel's; only the rows change, to skeletons - four shapes cycling (a horizon, a portrait, a pair, a close-up) drawn from the tokens and swept by the site's own shimmer, the one the report's placeholders use. Real photographs as decoys were rejected: eleven downloads nobody sees for 80ms each, to deliver motion a skeleton gives free. The winner's `<img>` is in the strip from the first frame, so the browser fetches it while the roll is still running and it is decoded by the time it stops. | built 2026-09-09 on the owner's ask ("there is nothing that spins... spinning gives a sense of slot machine playful vibes"), reversing the earlier call that a picture topic got no reel |
| Tongue twisters (the first warm-up) | (new) | `mocks/tongue-twisters.html` | A second round shape, drawn twice. **The first drawing was wrong and is recorded here because the error is the point**: it drew short twisters ("Peter Piper picked a peck") against the shipped ring clock, and the reference reels are neither. They are passages of forty seconds and up on a teleprompter, and the scroll is the timer. The second drawing follows them. **The scroll is the timer, so there is no clock.** The text moves upward at a speed you pick and you keep up; falling behind is the failure and you can hear it happen, so nothing needs to measure it, which is also why this costs nothing to run - no microphone, no transcript, no model, nothing against the allowance in `PRICING.md` §6. **Speed replaces difficulty**, which retires the first drawing's three pips and the open question behind them: the same passage is gentle at 120 and brutal at 220, and the person picks, so there is no column to author and nothing to defend. **The bank is passages, not one-liners**: 105 to 120 words, running 53 to 58 seconds at 120 and about 30 at 220, each built around one sound family, one word repeated until it loses its shape, or the long-word pile-up. **It is built to be screen-recorded**, which is what the reference creators actually do - record the screen, record the webcam, stack the two. So the stage is a narrow portrait column that survives a crop to 9:16 without losing a word, masked top and bottom so text arrives and leaves softly rather than being cut by a hard edge, with a three-two-one lead-in so somebody can press record and get their face ready before the first word arrives. The column is narrow on purpose, three or four words a line, which is what the reels land on and what a mouth already running at 180 can take in without the eye travelling. **The done screen claims only what it can know**: nothing was recorded, so it says the passage, the speed and the word count, and the person supplies the verdict they have already heard. Again at the next speed up is the primary button, the reverse of the round's done screen, because a warm-up is repetition rather than variety, and the last line is the conversion path into a real round. The quiet link under the buttons reads **"Challenge your friend" with a copy glyph** rather than "Copy a link so somebody can try this one" (owner's call): the old line described the mechanism, this one says why anybody would press it, and the glyph carries the mechanism instead, which is what a glyph is for. The underline sits on the words only, or the icon reads as part of the sentence rather than the thing you press. The link is the passage and its speed, so the dare is "I did this at 180, you try", which makes this the first surface that would spend the stateless link already drawn in `mocks/challenge.html`. **Easy and hard is a label, and both are free** (owner's call). Passage difficulty is real and separate from speed - the long-word pile-up at 150 is far harder than Peter Piper at 150, because the sounds differ rather than the pace - so the page leads with Easy and the passage screen names it. Paywalling hard was considered and dropped on mechanics rather than principle: speed already is the difficulty dial and it is free, so a content gate is routed around by running an easy passage at 220, and `SPEC.md` §3 pins with a test that every row's text is on its genre page, so the hard passages are readable free there whatever the gate does. What is sold instead is the rule already written, creation not access. **It gets a card on `/features`** (owner's call), one entry in `FREE_FEATURES` with a `mic` glyph, linking to `/tongue-twisters` rather than into the tool, because Streak and All genres already point at pages and this one has a page worth landing on. **About seven lines in the window, not thirteen** (owner's call: the first pass was cluttered): the type is sized off the viewport height rather than its width, since what is held constant is how many lines are on screen and that is a height question, and a width-led clamp bottomed out on a narrow window and put the wall of text back. Measured 67px and 6.8 lines at 1280x800, 37px and 12 lines on a 375px phone, which keeps more lines on purpose because three words a line beats one and the phone is the surface somebody practises on rather than films from. Measured at 1280x800 against the 740px budget: idle 308, passage 498, lead-in 261, done 362, and the reading stage bounded to exactly `100dvh` minus the header, since min-height alone left the height circular and the scroller had nothing to scroll inside. **Re-homed the same day on the owner's call: the round left home for `/tongue-twisters`.** Home is the tool and every new mode was going to be paid for out of that one screen, and the bill had already arrived - on home's settings sheet a warm-up left five of six controls doing nothing, which is the failure `SPEC.md` §3 names for styles, five times over. So a feature that changes what the round *is* now takes a URL, carries its own heading, button, settings and server-rendered bank, and appears in the picker as a link with a chevron rather than an option. **And the page opens on the tool** (owner's call): the server picks the passage, so the first paint is already the passage, its speed and Start, rather than a heading and a button in front of them. A feature page has no idle phase at all and the way out of a passage is another passage; the bank stays below the fold, which is what a crawler reads. That removed more than it added: the client fetch, the pending-deep-link mechanism and the picker's second group of options are all gone, and the feature page's passages are its crawlable content and its round's bank at once. | drawn 2026-09-12, redrawn the same day on the owner's note that the twisters were far too short and the ring was the wrong instrument, revised again with three calls settled (free at every difficulty, a card on `/features`, a cleaner reading screen), and **built the same day**. The three calls that were open were taken as recommended and are logged in `DECISIONS.md`: passages are fetched when the genre is picked rather than shipped inline, the stage is full bleed rather than an explicit 9:16 frame, and a warm-up does not build the streak. Two things the build found that no drawing could have: `Topic.text` was 200 characters wide and both the file loader and the Pro paste truncate to it, so every passage would have been silently cut mid-sentence; and `pool` fell back to the whole sentence bank before the passages had been fetched, which put "The day that never brightened" on a teleprompter in the browser. Both are fixed and pinned by test. Not yet drawn or built: whether a read-mode genre ships inline with the bank or is fetched when it is picked (recommended: fetched, since sixty passages is about 45KB of raw JSON and the 2026-09-09 decision treated 34KB as worth removing, while the instant-spin promise is about the topic reel rather than about a passage somebody has just chosen to read for a minute); whether the stage is full-bleed as drawn or draws an explicit 9:16 frame so a recording needs no cropping at all, which is better for the person recording and worse for the person practising on a laptop; and whether a warm-up counts towards the streak (drawn as no, which is the work the name is doing) |
