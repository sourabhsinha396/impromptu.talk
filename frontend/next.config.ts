import type { NextConfig } from "next";

/* Browser API calls use app/api/[...path], which forwards the session to the
   backend and returns its Set-Cookie headers to the browser. */
const nextConfig: NextConfig = {};

export default nextConfig;
